---
phase: 02-core-gui
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/ui/entry.rs
  - src/ui/timeline.rs
  - src/ui/state.rs
  - src/app.rs
  - src/db/entries.rs
autonomous: true

must_haves:
  truths:
    - "User can click entry to enter edit mode"
    - "User sees raw markdown in edit mode"
    - "User can save entry by pressing Escape"
    - "User can save entry by clicking outside"
    - "User can delete entry via right-click menu"
    - "Clearing all text deletes the entry"
    - "User can set duration on entry"
  artifacts:
    - path: "src/ui/entry.rs"
      provides: "Edit mode rendering"
      contains: "TextEdit::multiline"
    - path: "src/db/entries.rs"
      provides: "Duration and time update methods"
      contains: "update_entry_time"
  key_links:
    - from: "src/ui/entry.rs"
      to: "src/ui/state.rs"
      via: "editing_entry_id check"
      pattern: "editing_entry_id"
    - from: "src/ui/timeline.rs"
      to: "src/db/entries.rs"
      via: "delete_entry call"
      pattern: "delete_entry"
---

<objective>
Implement inline editing with click-to-edit, save-on-escape, context menu delete, and duration setting.

Purpose: Complete entry manipulation - users can edit, save, and delete entries.
Output: Working edit mode (DIARY-02), delete (DIARY-03, DIARY-04), duration (DIARY-07)
</objective>

<execution_context>
@/home/gregor/.claude/get-shit-done/workflows/execute-plan.md
@/home/gregor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-gui/02-RESEARCH.md
@.planning/phases/02-core-gui/02-02-SUMMARY.md

@src/ui/entry.rs
@src/ui/state.rs
@src/app.rs
@src/db/entries.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement click-to-edit and save behavior</name>
  <files>src/ui/entry.rs, src/ui/state.rs, src/ui/timeline.rs</files>
  <action>
1. In src/ui/entry.rs, create dual-mode render function:
   ```rust
   pub fn render_entry(
       ui: &mut egui::Ui,
       state: &mut DiaryViewState,
       entry: &DiaryEntry,
       cache: &mut CommonMarkCache,
   ) -> EntryAction
   ```
   where EntryAction is an enum: `None | Save(i64, String) | Delete(i64)`

2. Edit mode logic (when state.editing_entry_id == Some(entry.id)):
   - Show TextEdit::multiline for edit_buffer
   - Add time editor: small TextEdit for start_time (HH:MM format)
   - Add optional duration input (number field, minutes)
   - Request focus on first frame: `if response.gained_focus() == false { response.request_focus() }`
   - Exit on Escape: `ui.input(|i| i.key_pressed(Key::Escape))`
   - Exit on click outside: `response.lost_focus() && !response.has_focus()`
   - Return EntryAction::Save with buffer content

3. View mode logic (when not editing):
   - Render markdown with CommonMarkViewer
   - On click: set editing_entry_id = Some(entry.id), copy content to edit_buffer
   - Use `response.interact(Sense::click())` to detect clicks

4. In timeline.rs, handle returned EntryAction:
   - Save: call db.update_entry()
   - Delete: call db.delete_entry()

5. Add start_time_buffer and duration_buffer to DiaryViewState for editing time/duration
  </action>
  <verify>
1. Click entry - switches to textarea with raw markdown
2. Edit text, press Escape - saves changes
3. Changes persist after restart
  </verify>
  <done>Click-to-edit works, Escape saves and returns to view mode</done>
</task>

<task type="auto">
  <name>Task 2: Add context menu and delete-on-empty</name>
  <files>src/ui/entry.rs, src/ui/timeline.rs, src/db/entries.rs</files>
  <action>
1. Add context menu to entry view mode in entry.rs:
   ```rust
   response.context_menu(|ui| {
       if ui.button("🗑 Delete").clicked() {
           // Set pending_delete in state or return action
           ui.close_menu();
       }
       if ui.button("📋 Duplicate").clicked() {
           // Optional: for later
           ui.close_menu();
       }
   });
   ```

2. Check for empty content when saving:
   - In timeline.rs save handling: if content.trim().is_empty(), delete entry instead
   - Show brief toast/flash (optional) or just delete silently

3. Add db method for updating time/duration in entries.rs:
   ```rust
   pub fn update_entry_full(&self, id: i64, content: &str, start_time: &str, duration: Option<i32>) -> Result<()>
   ```

4. Wire duration editing:
   - In edit mode, show "Duration (min):" with number input
   - Parse and include in update_entry_full call
   - Display duration in view mode as end time calculation
  </action>
  <verify>
1. Right-click entry - context menu appears with Delete option
2. Click Delete - entry removed from timeline
3. Edit entry, clear all text, press Escape - entry deleted
4. Set duration on entry - end time displays correctly
  </verify>
  <done>Context menu delete works, empty-text delete works, duration editable</done>
</task>

</tasks>

<verification>
1. `cargo check` - no errors
2. Click entry - enters edit mode with focus
3. Press Escape - saves and returns to view
4. Click outside entry - saves and returns to view
5. Right-click entry - shows context menu
6. Delete via menu - entry removed
7. Clear text and save - entry deleted
8. Set duration - end time shown in view mode
</verification>

<success_criteria>
- DIARY-02: User can edit entry in place (raw markdown input, rendered display) ✓
- DIARY-03: User can delete entry via right-click context menu ✓
- DIARY-04: User can delete entry by clearing all text ✓
- DIARY-07: User can set optional duration on diary entry ✓
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-gui/02-03-SUMMARY.md`
</output>

