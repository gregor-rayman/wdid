---
phase: 02-core-gui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/mod.rs
  - src/ui/state.rs
  - src/ui/header.rs
  - src/main.rs
  - src/app.rs
  - Cargo.toml
autonomous: true

must_haves:
  truths:
    - "User sees today's date displayed in the app header"
    - "User can click left arrow to go to yesterday"
    - "User can click right arrow to go to tomorrow"
    - "Search box is visible in header area"
  artifacts:
    - path: "src/ui/mod.rs"
      provides: "UI module exports"
    - path: "src/ui/state.rs"
      provides: "DiaryViewState struct"
      contains: "current_date"
    - path: "src/ui/header.rs"
      provides: "Header rendering with nav and search"
      contains: "render_header"
  key_links:
    - from: "src/app.rs"
      to: "src/ui/state.rs"
      via: "DiaryViewState field in WdidApp"
      pattern: "DiaryViewState"
    - from: "src/ui/header.rs"
      to: "src/ui/state.rs"
      via: "state parameter"
      pattern: "state: &mut DiaryViewState"
---

<objective>
Establish UI module structure with DiaryViewState and implement the header bar with date navigation and search box.

Purpose: Creates the foundation for all Phase 2 UI work - state management and the persistent header that appears on every view.
Output: Working date navigation (NAV-01, NAV-02) and visible search box (SRCH-02 partial)
</objective>

<execution_context>
@/home/gregor/.claude/get-shit-done/workflows/execute-plan.md
@/home/gregor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-gui/02-RESEARCH.md

@src/app.rs
@src/db/entries.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UI module with DiaryViewState</name>
  <files>src/ui/mod.rs, src/ui/state.rs, src/main.rs, Cargo.toml</files>
  <action>
1. Add chrono dependency: `cargo add chrono@0.4`
2. Create `src/ui/mod.rs` exporting state and header submodules
3. Create `src/ui/state.rs` with DiaryViewState struct:
   ```rust
   pub struct DiaryViewState {
       pub current_date: chrono::NaiveDate,
       pub editing_entry_id: Option<i64>,
       pub edit_buffer: String,
       pub search_query: String,
       pub search_results: Option<Vec<crate::db::DiaryEntry>>,
   }
   ```
4. Implement Default for DiaryViewState (current_date = Local::now().date_naive())
5. Add `mod ui;` to src/main.rs

AVOID: Don't use chrono::Local in const context - use a new() function instead.
  </action>
  <verify>`cargo check` passes with no errors</verify>
  <done>DiaryViewState struct exists with all fields, Default impl returns today's date</done>
</task>

<task type="auto">
  <name>Task 2: Implement header with navigation and search box</name>
  <files>src/ui/header.rs, src/ui/state.rs, src/app.rs</files>
  <action>
1. Create `src/ui/header.rs` with `render_header(ui: &mut egui::Ui, state: &mut DiaryViewState)` function
2. Header layout (horizontal):
   - Left arrow button "◀" - calls `state.current_date = state.current_date.checked_sub_days(Days::new(1)).unwrap_or(state.current_date)`
   - Date label showing `state.current_date.format("%A, %B %d, %Y")` (e.g., "Saturday, February 08, 2026")
   - Right arrow button "▶" - similar with checked_add_days
   - Spacer (ui.with_layout Align::RIGHT)
   - Search text input bound to `state.search_query` with placeholder "🔍 Search..."
3. Add `pub mod header;` to ui/mod.rs
4. In src/app.rs:
   - Add `view_state: ui::state::DiaryViewState` field to WdidApp
   - Initialize in new() with DiaryViewState::default()
   - In update(): replace placeholder content with call to `ui::header::render_header(ui, &mut self.view_state)`

Use `ui.horizontal(|ui| { ... })` for the header row.
Use `egui::TextEdit::singleline(&mut state.search_query).hint_text("🔍 Search...")` for search.
  </action>
  <verify>
1. `cargo run` opens window
2. Header shows today's date
3. Arrow buttons change the displayed date
4. Search box is visible and accepts text input
  </verify>
  <done>Header displays with working date navigation arrows and visible search input</done>
</task>

</tasks>

<verification>
1. `cargo check` - no errors
2. `cargo run` - window opens with header bar
3. Click left arrow - date changes to yesterday
4. Click right arrow - date changes back to today
5. Type in search box - text appears (no search functionality yet)
</verification>

<success_criteria>
- NAV-01: User can navigate between days using arrow buttons ✓
- NAV-02: App displays single-day view (today by default) ✓
- SRCH-02: Dedicated search box visible (results in later plan)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-gui/02-01-SUMMARY.md`
</output>

