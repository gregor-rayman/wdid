---
phase: 02-core-gui
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/ui/timeline.rs
  - src/ui/entry.rs
  - src/ui/mod.rs
  - src/app.rs
  - Cargo.toml
autonomous: true

must_haves:
  truths:
    - "User sees list of diary entries for selected date"
    - "User can create entry by clicking timeline area"
    - "User can create entry with Ctrl+N"
    - "Entry times snap to 15-minute intervals"
    - "Entries display hashtags visually distinct"
  artifacts:
    - path: "src/ui/timeline.rs"
      provides: "Timeline rendering and entry creation"
      contains: "render_timeline"
    - path: "src/ui/entry.rs"
      provides: "Entry display with markdown"
      contains: "render_entry"
  key_links:
    - from: "src/ui/timeline.rs"
      to: "src/db/entries.rs"
      via: "db.get_entries_for_date call"
      pattern: "get_entries_for_date"
    - from: "src/ui/timeline.rs"
      to: "src/db/entries.rs"
      via: "db.save_entry for new entries"
      pattern: "save_entry"
---

<objective>
Implement the timeline view showing diary entries and entry creation via timeline click or Ctrl+N.

Purpose: Core diary functionality - users can see and create entries on any day.
Output: Working entry creation (DIARY-01) with time display and hashtag rendering (DIARY-08)
</objective>

<execution_context>
@/home/gregor/.claude/get-shit-done/workflows/execute-plan.md
@/home/gregor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-gui/02-RESEARCH.md
@.planning/phases/02-core-gui/02-01-SUMMARY.md

@src/app.rs
@src/ui/state.rs
@src/db/entries.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install egui_commonmark and create entry rendering</name>
  <files>Cargo.toml, src/ui/entry.rs, src/ui/mod.rs</files>
  <action>
1. Add dependency: `cargo add egui_commonmark@0.21`
2. Create `src/ui/entry.rs` with:
   - `render_entry_view(ui: &mut egui::Ui, cache: &mut CommonMarkCache, entry: &DiaryEntry)` function
   - Display time badge: `ui.label(format!("⏱ {}", entry.start_time))` 
   - If duration set, show end time too: `format!("{} - {}", start, end_time)`
   - Render content with CommonMarkViewer::new().show(ui, cache, &entry.content)
   - For hashtag styling: egui_commonmark handles #tags with default styling; they'll be visible in rendered markdown
3. Add `pub mod entry;` to ui/mod.rs
4. Add `markdown_cache: egui_commonmark::CommonMarkCache` field to WdidApp, initialize with CommonMarkCache::default()

Note: egui_commonmark 0.21 API: `CommonMarkViewer::new().show(ui, &mut cache, content)`
  </action>
  <verify>`cargo check` passes</verify>
  <done>Entry rendering function exists, markdown cache added to app</done>
</task>

<task type="auto">
  <name>Task 2: Create timeline with entry list and creation</name>
  <files>src/ui/timeline.rs, src/ui/mod.rs, src/app.rs, src/ui/state.rs</files>
  <action>
1. Create `src/ui/timeline.rs` with:
   - `render_timeline(ui, state, entries, db, cache)` function signature
   - Use egui::ScrollArea::vertical() for scrollable entry list
   - For each entry in entries (sorted by start_time):
     - Render time and content using render_entry_view
     - Add visual gap (ui.add_space(8.0)) between entries with 30+ min time gap
   - Add "Create entry" logic:
     - Keyboard: Check `ui.input(|i| i.consume_key(Modifiers::CTRL, Key::N))` 
       - Only if `!ui.ctx().wants_keyboard_input()` (not in text field)
     - Create with current time (snapped to 15 min)
   
2. Add time snapping helper in state.rs:
   ```rust
   pub fn snap_to_15_minutes(time: chrono::NaiveTime) -> chrono::NaiveTime {
       let minutes = time.minute();
       let snapped = ((minutes + 7) / 15) * 15;
       let snapped = snapped.min(45); // 60 -> 45
       chrono::NaiveTime::from_hms_opt(time.hour(), snapped, 0).unwrap()
   }
   ```

3. In src/app.rs update():
   - Load entries: `let entries = self.db.get_entries_for_date(&self.view_state.current_date.format("%Y-%m-%d").to_string())?`
   - Handle Ctrl+N for new entry creation (snap time, save to db, reload entries)
   - Call render_timeline after header

4. For new entry creation:
   - Get current time: `chrono::Local::now().time()`
   - Snap it: `snap_to_15_minutes(time)`
   - Create NewDiaryEntry with empty content, save via db.save_entry()

5. Add `pub mod timeline;` to ui/mod.rs
  </action>
  <verify>
1. `cargo run` - entries for current date appear in scrollable list
2. Press Ctrl+N - new entry created at current time (rounded to 15 min)
3. Navigate to different date, Ctrl+N creates entry on that date
  </verify>
  <done>Timeline displays entries, Ctrl+N creates new entries with snapped time</done>
</task>

</tasks>

<verification>
1. `cargo check` - no errors
2. `cargo run` - shows empty timeline (no entries yet)
3. Ctrl+N - creates entry, appears in timeline with time badge
4. Entry content (if any) renders as markdown
5. Navigate to yesterday, Ctrl+N - creates entry there
6. Return to today - original entry still there
</verification>

<success_criteria>
- DIARY-01: User can create diary entry with start time ✓
- DIARY-08: Hashtags visible in rendered markdown ✓
- Entries persist in database across restarts ✓
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-gui/02-02-SUMMARY.md`
</output>

